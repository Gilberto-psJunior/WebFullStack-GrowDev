// criar uma funcao chamda calcular media que recebe dois numeros como parametros e retorna a media desses numeros
function calcularMedia(num1, num2) {
    let media = (num1 + num2) / 2;
    return media;
}
let resultado = calcularMedia(10, 6);
console.log(resultado);
